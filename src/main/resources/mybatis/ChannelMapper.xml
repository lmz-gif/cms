<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.limengze.dao.ChannelMapper">

	<!-- 多对一查询查询频道 -->
	<select id="findChannelById" resultType="channel">
		 SELECT * FROM cms_channel WHERE id = #{value} limit 1
	</select>  
	
</mapper>  
  